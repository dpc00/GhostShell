"""No-op hook for the dynamic color-scheme init/save/repair pipeline.

Kept as a stub (rather than deleting 24 call sites in ai_terminal.py) so
tracing can be re-enabled by editing this one function.
"""


def color_scheme_log(message):
    pass
