{
  "saved_at": "2026-08-18, after ST restart, Grok session 01a012c9-158a-7732-b7c6-b6cfde409e98",
  "reason": "User cannot re-test Claude until 11pm. They then corrected Grok-as-proxy, stated no ST scrollback in Grok, said no-alt-screen is mandatory for mouseable long responses, and reported thinking/findings are not logged (Tab 2).",

  "sublime-mcp_repo_status": "Clean. All work from that session committed (3 commits: proof harness, production fixes for F1/F3-F11/F13, F11 live-runtime verification). AGENT_CHAT.md has full cross-agent handoff history. Nothing pending there.",

  "ghostshell_repo_status_uncommitted": {
    "what": "Three fixes for the resize/rewrap + status-line jiggle family. Long diagnosis in ai/TODO.md; landing notes in ai_terminal_notes.md (2026-08-18 entry). Same family as wrap_4_char_gap below, not superseded by it.",
    "files_changed": [
      "ai/terminal/layout.py - accepted_cols() hysteresis (never grow PTY width by exactly one column); follow_line_count(total, ignore_trailing) generic trailer skip",
      "ai/terminal/render.py - trim_display_rows(): last non-blank + blank cursor row only when it is the next line",
      "ai/ai_terminal.py, ai/terminal/__init__.py - wired accepted_cols, trim_display_rows, follow_ignore_trailing_lines into _LayoutWatcher / _do_render / _scroll_to_bottom",
      "ai_terminal.sublime-settings - follow_ignore_trailing_lines default 0; Claude-family profiles set to 2",
      "tests/test_layout.py, tests/test_trim_display.py",
      "ai/TODO.md, ai_terminal_notes.md"
    ],
    "verification": "211 unit tests OK. After the 2026-08-18 restart: accepted_cols live in-process; no 32<->33 chase at 123/124 or on 29/38 drags (console panel must be closed or the watcher will not measure). Idle caret no longer parks on the last line. follow_ignore_trailing_lines still needs a plugin reload to take effect. Strongest remaining check: ~32-col Claude pane with streaming output, line numbers on.",
    "do_not_put_in_the_engine": "No TUI-specific chrome matching (auto mode / accept edits / pause glyphs). An earlier draft that did that was removed. Profile settings only.",
    "not_done_on_purpose": "4-digit gutter reserve in _measure; update_replace_scroll / last-row overflow history append (needs a failing parser test on verbatim cast frames first); CCSTATUSLINE_WIDTH spawn env (upstream ccstatusline has no win32 width probe)."
  },

  "do_not_2026-08-18": [
    "Do not touch/save Packages/User/PluginLoader.py or GhostShell/PluginLoader.py.",
    "Do not assume a crash happened after restart -- check actual PIDs/process state first.",
    "Do not re-revert the layout.py/render.py fix without an explicit instruction from the user; it was already reverted once by mistake and had to be redone.",
    "Do not disable line numbers as 'the fix' -- that was the user's own manual workaround for symptom 1 before the real root cause (layout.py hysteresis) was found; the real fix should make line numbers safe to re-enable."
  ],

  "user_asks_open": [
    "Periodic screenshots: 628MB was useful; turn them back on. User then said 'no screenshots'.",
    "Wrapping leaves 4 blank chars on the right. 5 hours previously spent, to no avail. Do not repeat the failed -4.0*cw approach blindly.",
    "CLOSED 2026-08-18: Grok JSONL tail is live in jsonl_tail_transcripts. Thinking summaries land. User will not push encrypted_content (likely distillation lock). Do not try to decrypt or scrape fullscreen thinking.",
    "2026-08-17 live hello: Vibe/Kiro-v2/Junie verified in jsonl_tail_transcripts (not hook daily). Gemini CLI IneligibleTierError. Default Kiro is sqlite. Junie is the GhostShell tab currently next to Grok.",
    "No-alt-screen is mandatory for the user: the reason the terminal is in ST is to mouse long responses. Grok fullscreen defeats that. They currently PageUp/PageDown and read Tab 2."
  ],

  "grok_vs_claude_scrollback_2026-08-18": {
    "claude": "Primary screen (CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1). Linear transcript into real ST scrollback. SIGWINCH rewraps/replays the conversation; that can change ST line-number gutter digits and start the 32<->33 accepted_cols attractor.",
    "grok_fullscreen": "Enters ?1049h. force_main_screen only strips the display flag. Grok paints a fixed-region TUI. ST buffer is ~the pinned PTY grid (no linear ST scrollback). Line-number column cannot grow from a conversation replay because there is no replay.",
    "grok_stable_chrome": "Grok has a stable command line and statusline (no Claude footer jiggle). That does not satisfy Claude users and is not a proxy for either live-verify.",
    "alt_screen_never_is_still_fullscreen": "pager.toml already has [terminal] alt_screen = never. --no-alt-screen / alt_screen=never still counts as fullscreen (CUP on primary screen). Not Claude-style linear ST scrollback.",
    "actual_st_scrollback_lever": "Grok minimal mode: --minimal, /minimal, or [ui] screen_mode = minimal. Existing GhostShell profile: Grok Build --minimal. config.toml is pinned screen_mode = fullscreen so plain grok stays fullscreen.",
    "minimal_tradeoff": "16-color terminal palette; /theme /dashboard /timeline /find /jump hidden. Finished blocks go into native scrollback.",
    "user_rejected_minimal": "User did reject --minimal. Reason: not all the status indicators appear. 2026-08-18 later: they want 'minimal without minimal bells and whistles' — native ST scrollback (mouse long responses) without --minimal's stripped experimental UI (missing status, 16-color, hidden dashboard/timeline). That third mode does not exist in Grok. Do not re-offer stock --minimal.",
    "do_not": "Do not treat a quiet Grok pane as a Claude proxy. Do not call alt_screen=never no-alt-screen in the Claude sense. Do not push --minimal as if they have not tried it."
  },

  "thinking_not_logged_2026-08-18": {
    "user": "All thinking is lost, not getting logged; important context and findings disappear.",
    "tab2": "STLogs daily markdown (data/logs/YYYY-MM-DD.md) is prompt + final Grok answer + tools. No thinking.",
    "grok_jsonl": "chat_history.jsonl has type=reasoning with a short plaintext summary[] plus encrypted_content. Full thinking is not recoverable from disk. events.jsonl only has phase_changed streaming_reasoning (no text).",
    "stlogs_hooks": "~/.grok/hooks/stlogs.json has SessionStart/UserPromptSubmit/PrePostTool/Stop/SessionEnd. No AfterModel. AfterModel thinking capture in ai_log_server.py is Gemini-shaped (llm_response.candidates[].content.parts thought).",
    "stlogs_jsonl_tailer": "ai_logger.py skips Claude content type=thinking (continue). Qwen/Gemini flatteners drop part.thought. No Grok type=reasoning adapter.",
    "ghostshell_session_text": "SessionTextLog only writes lines that permanently retire from the PTY viewport. Fullscreen TUI thinking is painted then replaced; it never retires as ST lines.",
    "stlogs_ledger": "docs/agent-logging-status.md marks Grok Verified on prompt/response/tools. Thinking was never a completion criterion.",
    "live_verified_2026-08-18": "Next session after STLogs reload. Tab 2 is jsonl_tail_transcripts/2026-08-17.md. This turn's user line, Grok thinking summary, reply, and tools appeared as they happened.",
    "user_will_not_push": "Encrypted full thinking stays encrypted. User: not going to push it; perhaps to stop Chinese distillation. Treat as closed. Do not decrypt, do not scrape the TUI frame, do not add AfterModel workarounds.",
    "next": "Done. Summaries only. encrypted_content is out of scope."
  },

  "grok_jsonl_tail_missing_2026-08-18": {
    "user": "grok is not doing jsonl tail logging",
    "root_cause": "STLogs/lib/ai_logger.py never walks ~/.grok/sessions. Grok logging is hooks-only (stlogs-hook.cmd -> :9511 -> daily md). jsonl_tail_transcripts is Claude/Gemini/Codex/Qwen only.",
    "native_file": "~/.grok/sessions/<cwd-slug>/<session-id>/chat_history.jsonl",
    "do_not_tail": "events.jsonl, updates.jsonl, rewind_points.jsonl, prompt_history.jsonl",
    "adapter_also_missing": "_native_record_to_lines has no Grok branch. Claude formatter would drop Grok assistant/user/reasoning shapes.",
    "audit_false_comfort": "audit_agent_logging.py reports Grok native source exists; that is not evidence the tailer reads it."
  },

  "screenshots_root_cause_not_yet_fixed_in_process": {
    "file_flag": "STLogs/lib/ai_logger.py _PERIODIC_SCREENSHOTS_ENABLED = True (saved).",
    "live_process": "Still running the OLD imported module. lib/ai_logger.py is NOT a top-level ST plugin, so saving it does not reload PluginLoader and does not re-run plugin_loaded. Last plugin_loaded diagnostic is 01:29:34 with the old 'screenshots every 60s' string, BEFORE the flag flip. In-memory flag is still False.",
    "evidence": "Screenshot dir exists but 0 files. ai_diagnostics.log has only init/unload lines — zero SCREENSHOT_* / SCREENSHOT_FAILED lines. First tick after enable also only records the timestamp and waits 60s; even after a real reload, one minute of no further lib/ reloads is required unless first tick is changed to shoot immediately.",
    "win32_risk": "_screenshot_via_win32 uses PrintWindow on Sublime's own HWND. Comment says this can block on the main thread; they bounce to a worker, but PrintWindow on the host process can still hang/crash. If enabling actually starts capture, treat a subsequent crash as this path first.",
    "to_activate": "Must reload STLogs.PluginLoader (touch PluginLoader.py or restart ST). That is a live top-level plugin reload — crash risk. Do NOT save User/PluginLoader.py."
  },

  "wrap_4_char_gap": {
    "status": "NOT a bug to fix by giving the TUI more columns. Documented in _measure.",
    "visible": "Grok/Claude wrap ~4 cells before the ST view edge (black strip, then minimap).",
    "code": "cols = max(mn, int((usable_w - 4.0 * cw) / cw))",
    "why_the_tax_exists": "Smaller tax -> ST H-scrollbar appears -> viewport_extent changes -> watcher reports new cols to the PTY (SIGWINCH).",
    "claude_no_alt_screen": "A width SIGWINCH makes Claude (force_main_screen) rewrap/replay the whole visible conversation (~1000 lines). That replay changes layout, reports width again, repeats ~10 times.",
    "live_2026-08-15": "User resized the tab. Scrolling back, the conversation is wrapped to the NEW size. No H-scrollbar because of the 4-char tax. They stated: if we reduce the tax, the H-bar WILL appear after that rewrap. So the gap is not only 'startup oscillation' -- it is required after every legitimate resize+rewrap as long as ST charges ~3 extra layout cells.",
    "real_fix": "Stop ST from adding those extra layout cells, or hide the H-bar, THEN we can report a view-filling width. The TUI will rewrap to it and still not get a bar. Do not send more columns while ST still charges +3.",
    "do_not": "Do not reduce 4.0 as the fix. Do not report a wider PTY size. Do not use word_wrap=True (extra visual row -> V-scrollbar)."
  },

  "do_not": [
    "Do not save Packages/User/PluginLoader.py.",
    "Do not assume a crash happened — check PIDs first.",
    "Do not re-disable screenshots without saying so.",
    "Do not 'fix' wrap by only changing 4.0 to 3.0 and calling it done."
  ],

  "stext_stuser_rule": "pybackup LocalCopy is bidirectional between projects/SText and Packages/User for *.py / sublime-commands / menus / keymaps / settings / color-schemes. A one-sided delete is restored on the next run. Dual-delete both sides. 2026-08-15: removed SText/logs and User/logs and User/AiLog.sublime-syntax; platform_config.py excludes logs/* both ways. Palette 'Ai: Screenshot at Scroll' removed from SText and User; only STLogs keeps it. STLogs is a separate junction, not on that arc.",

  "logging_ownership_2026-08-15_evening": "User clarified the last detail: everything GhostShell CALLS that is a logging function belongs IN GhostShell, not STLogs. Moved cast_recorder, session_text_log, log_paths, color_scheme_log, settings_debug_log, raw_debug_log back to GhostShell/ai/terminal/. GhostShell has zero STLogs imports. STLogs keeps conversation/hook/JSONL/screenshot logging only. Tests: GhostShell 387, STLogs 1. Not committed."
}
