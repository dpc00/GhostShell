  Not applicable: GPU/Metal/OpenGL rendering, custom shaders, native macOS/GTK shell, quick-terminal dropdown, terminal inspector
  GUI, sixel/Kitty graphics protocol, tmux integration, split-pane windowing, crash telemetry — these all assume Ghostty's
  standalone GPU-surface app model, which doesn't map onto rendering inside Sublime Text's text buffer.
